//The files evaluation Theory
/**
 * @brief 
 * The sequenctial files evaluation theorem
 * The representaion
 * of the residence
 * We will have this
 * 
 */
//The mainn classes and methods to be USED to read data from a file
# include<iostream>
using namespace std;


int main()
{
    int i;
    cin>>i;
    cout<<i<<endl;

    std::fstream in_file("./Hello.txt",std::ios::in | std::ios::binary);

     
}

// The bit wise or operator
// It sets both bit and binary mode to true
// The if stream class
/**
 * @brief We have the following
 *
 */

//fstream and ifstream are both used for input files purpose
/**
 * @brief 
 * Create an fstream or an ifstream object
 * Connect it to a file on a file system
 * Read data from file via the file stream
 * close the stream
 * 
 * The closing of the Stream is necessary
 * 
 */