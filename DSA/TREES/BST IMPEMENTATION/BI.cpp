# include<iostream>
using namespace std;


class Node
{
    public:
    int data;
    Node *left;
    NOde *right;

    Node(int a)
    {
        this->data = data;
        this->left = NULL;
        this->right = NULL;
    }
};

Node * InsertIntoBst(Node *&root,int d)
{
    if(root == NULL)
    {
        root = new Node(d);
        return root;
    }

    if()>root->data)

}


void TakeInput(Node * & root)
{
    int data;
    cin>>data;
    while(data!=-1)
    {
        root = InsertIntoBst(root,data);
        cin>>data;
    }

}


int main()
{
    
}

// THE BST

// We will find the mid oiint of the particular arraya

// Then we will find the left portiona dn the right portion

// Do searching on the left most portion and the right ost portion

//

// Mod pointis the middle mosr element

// We will focus on the
// Sorted arrays
// Tehe searching time for this would be log(n)

// In case of the linear search we will have t

// In small time we can sesearch te element

//We will have two nodes 

/**
 * @brief 
 * Now we will have 
 * 
 * if we have a noode 
 * 
 * then if root -> data 
 * 
 * Greater part Right most part would be roght -->> 
 * Smaller Part If the number to be inserted is smaller than the given element 
 * 
 * 
 */